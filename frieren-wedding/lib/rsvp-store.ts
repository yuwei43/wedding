import { type ResultSetHeader,type RowDataPacket } from 'mysql2/promise';
import { database } from './server';
import { AppError,type RsvpInput,type RsvpRecord } from './rsvp-domain';
export type Row=RowDataPacket&{id:string;credential_hash:string;name:string;phone:string;guests:number;needs_stay:number;stay_guests:number;check_in:string|null;check_out:string|null;notes:string;status:RsvpRecord['status'];created_at:string;updated_at:string};
export function publicRecord(row:Row):RsvpRecord{return {id:row.id,name:row.name,phone:row.phone,guests:row.guests,needsStay:!!row.needs_stay,stayGuests:row.stay_guests,checkIn:row.check_in,checkOut:row.check_out,notes:row.notes,status:row.status,createdAt:row.created_at,updatedAt:row.updated_at,consent:true};}
export async function ownRecord(hash:string){const [rows]=await database().execute<Row[]>('SELECT * FROM rsvps WHERE credential_hash=? LIMIT 1',[hash]);return rows[0]||null;}
export async function createRecord(hash:string,x:RsvpInput){const now=new Date().toISOString(),id=crypto.randomUUID();await database().execute<ResultSetHeader>('INSERT INTO rsvps (id,credential_hash,name,phone,guests,needs_stay,stay_guests,check_in,check_out,notes,status,consent_at,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?) ON DUPLICATE KEY UPDATE credential_hash=credential_hash',[id,hash,x.name,x.phone,x.guests,Number(x.needsStay),x.stayGuests,x.checkIn,x.checkOut,x.notes,x.needsStay?'pending':'none',now,now,now]);const row=await ownRecord(hash);if(!row)throw new AppError('回执未保存，请重试。',503);return publicRecord(row);}
export async function updateRecord(row:Row,x:RsvpInput,adminStatus?:unknown){let status:RsvpRecord['status']=x.needsStay?'pending':'none';const unchanged=!!row.needs_stay===x.needsStay&&row.stay_guests===x.stayGuests&&row.check_in===x.checkIn&&row.check_out===x.checkOut;if(x.needsStay&&unchanged)status=row.status==='confirmed'?'confirmed':'pending';if(adminStatus!==undefined){if(!['pending','confirmed','none'].includes(String(adminStatus)))throw new AppError('住宿状态无效。');if(x.needsStay&&adminStatus==='none')throw new AppError('需要住宿的宾客请选择待联系或已确认。');if(!x.needsStay&&adminStatus!=='none')throw new AppError('无需住宿的宾客应选择无需安排。');status=adminStatus as RsvpRecord['status'];}const now=new Date().toISOString();const [result]=await database().execute<ResultSetHeader>('UPDATE rsvps SET name=?,phone=?,guests=?,needs_stay=?,stay_guests=?,check_in=?,check_out=?,notes=?,status=?,updated_at=? WHERE id=?',[x.name,x.phone,x.guests,Number(x.needsStay),x.stayGuests,x.checkIn,x.checkOut,x.notes,status,now,row.id]);if(!result.affectedRows)throw new AppError('这份回执已不存在。',404);const [rows]=await database().execute<Row[]>('SELECT * FROM rsvps WHERE id=? LIMIT 1',[row.id]);return publicRecord(rows[0]);}
